{{#if showAuthorControls}}
<span class="mod_tools" data-is-author="{{showAuthorControls}}">
    <a href="#" class="control mod_edit" data-key="{{key}}">bewerk</a>
    | <a href="#" class="control mod_delete" data-key="{{key}}">verwijder</a>
    <span class="message"></span>
</span>
{{/if}}
