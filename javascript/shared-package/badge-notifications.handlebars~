<h3>Nieuwe badge{{plural this.length}} <span style="font-weight:normal; float: right; font-size: 0;">(<a href="javascript:void(0)" class="hide-badge">sluit</a>)</span></h3>
{{#each this}}
    <div class="badge-container" style="margin-bottom:5px;">
        <a href="{{relativeUrl}}" class="big-button" target="_blank">
        	{{> shared_badge}}
        </a>
        <div>
        {{> shared_share-links}}
        </div>
    </div>
{{/each}}
